# Night Drive — 3D 나이트시티 개발 계획

브라우저에 접속하면 운전석 시점(1인칭)으로 **하나의 거대한 도시**를 끝없이 달리는 웹사이트.
v1(픽셀 렌더러, 시골/도시/고속도로 바이옴)을 폐기하고 **three.js 실시간 3D**로 다시 만든다.
맵과 외관은 *Cyberpunk 2077*의 **나이트시티**를 베이스로 한다. 구역 배치·랜드마크·색·간판·안개·홀로그램 광고까지 "그 도시"로 읽혀야 한다.

> 2026-09-09: v2 착수. v1 픽셀 코드는 git 히스토리(`b4b2b47` 이전)에만 남긴다.
>
> **진행 상황 (2026-09-09)**: M0~M5 1차 구현 완료, M6 반복 튜닝 진행 중. 구현됨: 16개 구역 + 배드랜즈 단일 맵, 22 km 순환 루트(모든 구역 경유), 인스턴스 건물 셰이더(8 스타일, 창문 클러스터·상점·슬래브·패널·높이 안개), 간판/홀로/브랜드/광고/육교 표지판 아틀라스, 랜드마크(아라사카 타워·기업 타워·메가빌딩 H10/H8·스타디움·몰·발전소·저택·카부키 시장·부두), 고가 고속도로(기둥·방음벽·마커), 지형 컷·바다·하늘, 가로등·프롭 18종, 주차 차량, 보행자, 차량·AV 교통(언더글로우), 거리 위 구조물, 콕핏(백밀러 렌더타깃·클러스터), 블룸·색보정, 비·스모그, 낮/노을/밤, 설정 패널, 자체 광고 전광판(클릭) + AdSense 오버레이, 스크린샷 검증 도구. 남은 것: §7 체크리스트 기준 추가 튜닝(외벽 디테일, 노스오크 저택·나무 밀도, 항만 크레인 밀도), AdSense ID.

---

## 1. 목표와 판정 기준

1. **하나의 맵.** 바이옴 스위치가 없다. 도시는 한 장의 지도이고, 차는 그 위의 긴 순환 루트를 돈다. 설정 패널은 "구역으로 이동"만 제공한다.
2. **나이트시티 지도.** 실제 게임의 구역 배치(§3)를 거의 그대로 재현한다. 구역마다 건물 밀도·높이·색·간판·조명이 다르고, 랜드마크가 있어야 "여기가 어디"인지 보인다.
3. **나이트시티 룩.** 밤 기준: 청록·마젠타·노랑 네온, 붉은 아라사카, 나트륨 주황 가로등, 보랏빛 스모그 안개, 블룸, 홀로그램 광고, 비 오는 젖은 노면. 낮/노을 프리셋은 같은 지오메트리에 조명·안개·하늘만 바꾼다.
4. **검증은 스크린샷.** `scripts/shot.mjs`(헤드리스 Edge)로 각 구역 스크린샷을 찍고 실제 게임 장면과 비교하며 "거의 흡사하다"고 판단될 때까지 반복한다. 기준 항목은 §7.
5. **광고 슬롯은 유지.** 자체 광고(매니페스트)는 3D 전광판·홀로그램에 텍스처로 붙고 클릭(레이캐스트)하면 링크가 열린다. AdSense는 DOM 오버레이 슬롯 1개로 유지.
6. **정적 배포.** GitHub Pages(Actions) 그대로. 서버 없음.

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 렌더 | three.js 0.186 (WebGL2) | 인스턴싱, 포스트프로세싱(UnrealBloom), 렌더 타깃(미러) |
| 빌드 | Vite + TypeScript | 유지 |
| 지오메트리 | **전부 절차 생성** | 외부 모델·텍스처 없음. 건물은 InstancedMesh + 커스텀 셰이더로 창문·네온 스트립을 프래그먼트에서 그림 |
| 텍스트/간판 | CanvasTexture 아틀라스 | 브랜드·가타카나·한자 간판, 광고 텍스처 |
| 포스트 | EffectComposer: Render → UnrealBloom → Grade(비네트, 색보정, 색수차, 그레인) | CP2077 특유의 번쩍이는 네온과 필름 룩 |
| 난수 | mulberry32 (v1 유지) | 시드 = 같은 도시 디테일 |
| 오디오 | v1 `audio.ts` 유지(엔진·노면·비) | |
| 검증 | playwright-core + Edge(`--use-angle=swiftshader`) | 헤드리스 WebGL 스크린샷 |
| 테스트 | Vitest: 구역 판정, 루트 스플라인, PRNG | 순수 로직만 |

---

## 3. 지도 (Night City)

좌표계: 미터. 원점 = 코포 플라자 중앙. **+x 동, -z 북**. 맵 반경 약 3 km, 그 밖은 배드랜즈.
서쪽은 태평양, 북서쪽은 델 코로나도 만(Del Coronado Bay).

```
                     N
        [Northside 공업/부두]   [Arasaka Waterfront]
   만   [Little China] [Kabuki]              [North Oak 언덕·저택]
        ──────────── 고가 고속도로 ────────────
   바다 [Corpo Plaza][Downtown] [Japantown][Charter Hill]
        ─────────────────────────────────────
   바다 [Wellsprings][The Glen][Vista del Rey] [Arroyo 공업]
                                              [Rancho Coronado 주택]
   바다 [Pacifica: Coastview 폐리조트 / West Wind]        배드랜즈
                     S (배드랜즈, 유전, 풍력)
```

| 구역 | 범위(x, z) | 특징 (건물 / 조명 / 간판) |
|---|---|---|
| **Corpo Plaza** | -900..-100, -450..450 | 유리·검은 강철 초고층, 아라사카 타워(최고층, 검정+붉은 로고), 밀리테크·강타오·페트로켐 타워, 메모리얼 광장. 차갑고 정돈됨. 청백 조명 |
| **Downtown** | -100..600, -500..500 | 빽빽한 고층, 홀로그램 광고탑, 곤페키 플라자. 마젠타·시안 간판 최대 밀도 |
| **Little China** | -800..200, -1250..-500 | 중층 아파트, 붉은 등·한자 간판, 메가빌딩 H10 |
| **Kabuki** | 200..950, -1450..-550 | 저·중층 밀집, 시장 회전교차로, 가타카나 네온, 파이프·에어컨 실외기 |
| **Northside** | -950..650, -2300..-1250 | 공장, 창고, 크레인, 컨테이너, 나트륨 주황등, 굴뚝 |
| **Arasaka Waterfront** | 650..1500, -2300..-1400 | 부두, 대형 크레인, 아라사카 창고(붉은 표식) |
| **Japantown** | 600..1400, -550..250 | 수직 네온 간판, 체리블로섬 마켓 등롱, 메가빌딩 H8, 핑크·빨강 |
| **Charter Hill** | 1400..2100, -800..300 | 고급 유리 고층, 흰·금색 조명, 널찍함 |
| **North Oak** | 1300..2700, -2300..-800 | 언덕 지형, 저택, 아라사카 저택, 드문 조명 |
| **Wellsprings** | -1150..-250, 500..1450 | 해안, 부두, 운하, 야자수, 중층 |
| **The Glen** | -250..650, 500..1450 | 시청, 중층 오피스, 절제된 조명 |
| **Vista del Rey** | 650..1450, 500..1450 | 저층 아파트, 노란 가로등, 낡음 |
| **Arroyo** | 1450..2500, 250..1350 | 발전소(냉각탑), 탱크, 굴뚝, 파이프 |
| **Rancho Coronado** | 1450..2700, 1350..2500 | 격자 단독주택, 드문 조명 |
| **Coastview (Pacifica)** | -1450..-450, 1500..2700 | 미완성 콘크리트 골조, 크레인, 그랜드 임페리얼 몰, 스타디움, 거의 소등 |
| **West Wind Estate** | -450..300, 1500..2500 | 저층 아파트 단지, 드문 조명 |
| **Badlands** | 그 밖 | 사막, 풍력 터빈, 펌프잭, 태양광, 멀리 도시 빛 |

랜드마크(고정 좌표): 아라사카 타워, 코포 플라자 메모리얼, 밀리테크·강타오·페트로켐, 곤페키 플라자, 메가빌딩 H10·H8, 카부키 시장탑, 스타디움, 그랜드 임페리얼 몰, 냉각탑, 아라사카 저택, 고가 고속도로 링, 해안 도로.

**드라이브 루트**: 웰스프링스 북단(아라사카 타워 정면) → 코포 플라자 → 다운타운 → 고가로 북상 → 리틀차이나 → 카부키 → 노스사이드 부두 → 아라사카 워터프런트 → 고가 → 재팬타운 → 차터힐 → 노스오크 언덕 → (고속도로) 아로요 → 란초 코로나도 → 배드랜즈 → 고가 → 비스타 델 레이 → 더 글렌 → 웨스트 윈드 → 다리 → 코스트뷰 → 해안 → 웰스프링스. 약 22 km 순환, Catmull-Rom 스플라인. 고가 구간은 높이 +12 m. 테스트가 모든 구역 경유·물 회피·경사 완만함을 보장한다.

---

## 4. 시스템

### 4.1 월드 생성 (`src/city`)
- `map.ts`: 구역 사각형/다각형, 해안선, 지형 높이 `h(x,z)`(노스오크·배드랜즈 언덕), `districtAt(x,z)`.
- `route.ts`: 경유점 → 스플라인, 등간격 샘플(2 m), 각 샘플의 위치·접선·높이·구역·도로 종류(street/highway/coast/tunnel).
- `generator.ts`: 구역 격자(블록 40~90 m)를 돌며 건물 인스턴스 생성. 루트 코리도(폭 24 m) 안은 비운다. 구역 프리셋으로 높이 분포·밀도·색·창문 밀도·네온 확률·간판 확률 결정. 결과는 InstancedMesh 데이터.
- `landmarks.ts`: 고정 좌표에 특수 지오메트리 조립(아라사카 타워, 메가빌딩, 냉각탑, 스타디움, 크레인, 풍력).
- `roads.ts`: 루트 리본(차선 텍스처), 고가 기둥·방음벽·조명 스트립, 도시 바닥(어두운 아스팔트 + 보도 + 격자 도로 라인), 가로등.
- `props.ts`: 네온 간판(수직/수평), 홀로그램 광고 판(스크롤 텍스처, 반투명 가산), 옥상 안테나·항공장애등, 굴뚝·탱크, 야자수.
- `traffic.ts`: 루트 위 차량(앞차 미등, 반대 차선 전조등) + 도시 상공 AV(에어로다인) 비행 레인.
- `sky.ts`: 돔 셰이더(수평선 스모그 → 위쪽 남색), 별, 달, 낮/노을 색.
- `ads.ts`: 매니페스트 → CanvasTexture. 전광판 인스턴스에 배정. 클릭 레이캐스트.

### 4.2 카메라·주행
- 차는 스플라인을 일정 속도로 따라감(설정 `speed`). 카메라 높이 1.15 m, 룩어헤드 12 m, 커브에서 약간 롤·요.
- 콕핏(`src/cockpit`): 카메라 자식으로 대시보드·핸들·A필러·백밀러(렌더타깃 후방 카메라)·HUD(속도, 구역명, CP2077식 노란 숫자).

### 4.3 시간·날씨
- `night`(기본)/`dusk`/`day`/`auto`/`cycle`. 프리셋은 안개색·밀도, 하늘, 앰비언트, 창문/네온 발광 강도, 블룸 세기를 바꾼다.
- `rain`: 파티클 + 노면 반사 강화 + 와이퍼 모션(HUD). `fog`: 밀도 2배.

### 4.4 광고
- 자체 광고: `public/ads/manifest.json` 유지. 이미지/텍스트 → 캔버스 텍스처. 큰 전광판·홀로 광고탑에 배정. 클릭 시 새 탭.
- AdSense 오버레이: 다운타운 "광고 벽" 통과 시 화면 우측 상단 고정 사각형에 DOM iframe(원본 그대로). 캔버스 요소가 덮지 않도록 HUD 안전영역 지정.

### 4.5 성능
- 건물 ≤ 8,000 인스턴스, 프롭 ≤ 20,000 인스턴스 단일 드로우콜 각각. 안개 너머(2.2 km)는 그리지 않음(frustum+거리 컬링은 인스턴스 셰이더에서 알파 클립 없이 안개로 가림).
- DPR ≤ 1.5, 모바일은 블룸 해상도 절반.

---

## 5. 모듈 구조

```
src/
  main.ts            # 부트스트랩, 루프, 리사이즈, 설정 연결
  tokens.ts          # 색·치수 토큰(DESIGN_SYSTEM.md)
  core/rng.ts        # 시드 PRNG
  city/ map.ts route.ts generator.ts buildings.ts landmarks.ts roads.ts props.ts terrain.ts sky.ts traffic.ts ads.ts
  cockpit/cockpit.ts
  fx/post.ts fx/weather.ts
  ui/settings.ts
  audio.ts
scripts/shot.mjs     # 검증용 스크린샷
```

---

## 6. 마일스톤

- **M0 뼈대**: three.js 씬, 루프, 리사이즈, 포스트 체인, 스크린샷 도구 — 검증: 안개 낀 밤 지평선.
- **M1 지도·루트**: 구역·지형·해안·루트 스플라인, 바닥/도로/고가 — 검증: 루트 전체 워프 스크린샷.
- **M2 건물·조명**: 인스턴스 건물 셰이더(창문·스트립), 구역 프리셋, 가로등, 하늘 — 검증: 다운타운·카부키 대비.
- **M3 랜드마크·간판·홀로**: 아라사카 타워, 메가빌딩, 네온·홀로 광고, 공업/항만 프롭 — 검증: 코포 플라자 실루엣.
- **M4 콕핏·교통·날씨·오디오**: 대시보드, 미러, HUD, 차량, AV, 비 — 검증: 주행감.
- **M5 광고·UI·배포**: 매니페스트 전광판, 클릭, 오버레이, 설정 패널(구역 이동), Pages 배포.
- **M6 반복 튜닝**: §7 체크리스트로 구역별 스크린샷 비교 → 수정. "흡사" 판정까지 반복.

---

## 7. "나이트시티 흡사" 체크리스트

| # | 항목 | 확인 방법 |
|---|---|---|
| 1 | 코포 플라자에서 아라사카 타워가 지배적으로 보이고 붉은 로고가 읽힌다 | corpo 스크린샷 |
| 2 | 다운타운은 홀로그램·간판이 화면의 30% 이상을 채우고 마젠타·시안이 주조 | downtown |
| 3 | 카부키·재팬타운은 저층 밀집 + 가타카나 수직 네온 + 붉은/핑크 | kabuki, japantown |
| 4 | 노스사이드·아로요는 주황 나트륨등, 굴뚝, 탱크, 크레인 | northside, arroyo |
| 5 | 퍼시피카는 어둡고 미완성 골조·크레인, 스타디움 실루엣 | pacifica |
| 6 | 배드랜즈는 텅 빈 사막 + 멀리 도시 빛 + 풍력 | badlands |
| 7 | 고가 고속도로에서 스카이라인 전체가 안개 속 실루엣 + 창 불빛 | highway |
| 8 | 안개는 보랏빛 스모그, 블룸은 네온만 번지고 노면은 젖은 반사 | 전 구역 |
| 9 | 하늘에 AV가 날고, 옥상 항공장애등이 깜빡인다 | 전 구역 |
| 10 | 콕핏 HUD가 CP2077 느낌(노란 숫자, 얇은 선) | 전 구역 |

---

## 8. 결정 사항

1. 실제 게임 에셋은 쓰지 않는다. 전부 절차 생성·코드 그림. 이름(Arasaka 등)은 오마주로만 텍스트에 쓴다.
2. 좌핸들·우측통행 유지.
3. v1의 픽셀 격자 원칙·팔레트 시스템은 폐기. DESIGN_SYSTEM.md는 3D 토큰(색·안개·발광·치수)으로 대체.
4. 운전은 자동. 방향키 조작 없음.
