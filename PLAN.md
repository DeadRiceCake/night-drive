# Night Drive — 개발 계획

브라우저에 접속하면 운전석 시점(1인칭)으로 끝없이 달리는 도로가 나오는 웹사이트.
모든 그래픽은 **하나의 저해상도 픽셀 격자** 위에 그려지며, 격자가 절대 어긋나지 않는다.
풍경은 시드 기반으로 무작위 생성되고, 시간대(낮/밤)와 지역(시골/도시)을 고를 수 있다.
길가의 전광판·현수막은 광고 슬롯으로 설계해 나중에 실제 광고를 붙인다.

---

## 1. 핵심 설계 원칙

### 1.1 "격자가 어긋나지 않는다"를 보장하는 방법

픽셀아트가 깨지는 원인은 항상 같다: 실수 좌표, 회전, 비정수 스케일, 안티앨리어싱, 서로 다른 해상도의 레이어 혼합.
이를 구조적으로 막기 위해 다음을 **규칙**으로 고정한다.

| 규칙 | 내용 |
|---|---|
| 단일 백버퍼 | 모든 것을 `320×180` (16:9) 픽셀 버퍼 하나에 그린다. UI·계기판·광고 프레임도 예외 없음 |
| 정수 업스케일 | 백버퍼를 창 크기에 맞는 **정수 배율**(×3, ×4, ×6 …)로만 확대. 남는 영역은 레터박스 |
| 소프트웨어 래스터라이저 | Canvas 2D의 `drawImage` 대신 `Uint8ClampedArray`에 직접 픽셀을 쓴다. 안티앨리어싱·서브픽셀 보간이 원천적으로 불가능 |
| 정수 좌표만 | 월드 → 스크린 투영 결과는 마지막에 `Math.floor`/`round`로 스냅. 스프라이트는 정수 위치에만 찍힘 |
| 회전·자유 스케일 금지 | 원근에 따른 크기 변화는 **이산 스케일 단계**(예: 1/8, 1/4, 1/2, 1, 2)로 미리 리샘플된 스프라이트를 선택. 핸들의 미세한 회전도 프레임 스프라이트로 처리 |
| 팔레트 렌더링 | 픽셀은 색이 아니라 **팔레트 인덱스**로 저장 → 마지막에 LUT로 RGB 변환. 낮/밤 전환이 팔레트 교체 한 번으로 끝나고, 조명 효과도 인덱스 시프트로 격자를 유지한 채 구현 |
| 디더링만 허용 | 그라데이션(하늘, 안개, 조명)은 알파 블렌딩 대신 베이어(Bayer) 디더 패턴으로 표현 |

기본 해상도는 `320×180`으로 시작하고, 필요하면 `480×270`으로 올린다(둘 다 1080p/4K에 정수 배율로 맞음).

### 1.2 의사 3D 도로 (Outrun 방식)

참고 영상처럼 "차 안에서 도로를 바라보는" 구도는 진짜 3D가 필요 없다.
1980~90년대 아케이드 레이싱의 **세그먼트 기반 스캔라인 도로**를 쓴다.

- 도로는 Z축 방향 세그먼트 목록(각각 곡률 `curve`, 높이 `y`)으로 정의.
- 매 프레임, 화면 아래쪽부터 **한 줄(스캔라인)씩** 도로 폭·중심 X·색 밴드를 계산해 채운다. 스캔라인 단위라서 격자와 자연스럽게 일치한다.
- 언덕·커브는 세그먼트 누적값으로 화면 X/Y가 휘어지는 것이라 별도 투영 행렬이 없다.
- 도로 옆 오브젝트(가로등, 나무, 전광판)와 차량은 세그먼트에 붙어 있고, 해당 세그먼트의 스크린 투영 값으로 위치·스케일 단계를 정한다.
- 백밀러/사이드미러는 **뒤쪽 세그먼트**를 같은 알고리즘으로 작게 렌더한 뒤 좌우 반전해 거울 영역에 블릿한다.

### 1.3 레이어 구성 (뒤 → 앞)

1. **하늘** — 팔레트 디더 그라데이션. 밤에는 별 필드 + 은하(참고 이미지의 핵심 요소) 스프라이트. 별은 시드 기반으로 배치, 느리게 시차 이동
2. **원경** — 산맥 / 도시 스카이라인 / 논밭 지평선. 2~3겹 시차(parallax) 스크롤. 커브 시 좌우로 이동
3. **도로 + 노면** — 스캔라인 렌더. 차선, 갓길, 가드레일, 중앙분리대
4. **도로변 오브젝트** — 가로등, 전봇대, 나무, 표지판, 건물, 육교, **전광판/현수막(광고 슬롯)**
5. **차량** — 앞차(미등), 반대 차선 차(전조등), 추월 차량
6. **조명 효과** — 밤에 전조등 원뿔, 미등 번짐: 팔레트 인덱스 시프트 + 디더
7. **콕핏(운전석)** — 대시보드, 계기판(속도계/RPM 바늘은 프레임 스프라이트), 핸들, A필러, 백밀러, 사이드미러. 참고 이미지처럼 화면 하단 1/3과 좌우 가장자리를 차지
8. **HUD(선택)** — 설정 버튼 등 최소 UI. 화면 밖 DOM으로 두고 필요 시 노출

---

## 2. 기술 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 빌드 | Vite + TypeScript | 빠른 개발, 정적 배포 |
| 프레임워크 | 없음 (바닐라) | 렌더 루프 하나가 전부. 설정 패널만 소량 DOM |
| 렌더 | Canvas 2D `putImageData` + CSS `image-rendering: pixelated` | 320×180 소프트웨어 렌더는 JS로 60fps 여유. WebGL은 나중에 업스케일 셰이더(CRT 등)가 필요할 때만 |
| 상태 | URL 쿼리 + `localStorage` | `?time=night&scene=city&seed=1234`로 공유 가능 |
| 스프라이트 | Aseprite(또는 LibreSprite) → PNG + JSON | 팔레트 인덱스 PNG를 로드 시 인덱스 버퍼로 변환 |
| 난수 | 시드 기반 PRNG (mulberry32 등) | 같은 시드 = 같은 풍경 |
| 오디오(선택) | Web Audio | 엔진음 루프 + 바람. 첫 클릭 후 재생 |
| 배포 | Cloudflare Pages / GitHub Pages / Vercel | 정적 파일 |
| 테스트 | Vitest | 도로 투영, PRNG, 청크 생성 등 순수 로직 |

---

## 3. 모듈 구조

```
src/
  main.ts                 # 부트스트랩, 루프, 리사이즈(정수 배율 계산)
  core/
    framebuffer.ts        # 인덱스 버퍼(Uint8Array 320×180) + 팔레트 LUT → ImageData
    blit.ts               # 스프라이트 블릿, 클리핑, 수평 반전, 디더 채우기
    palette.ts            # 팔레트 정의, 시간대별 램프(ramp), 인덱스 시프트 테이블
    rng.ts                # 시드 PRNG
    loop.ts               # 고정 타임스텝(60Hz) + 렌더
    assets.ts             # 스프라이트시트/JSON 로더, 스케일 단계 프리컴퓨트
  road/
    segments.ts           # 세그먼트 데이터, 곡선/언덕 생성기
    projection.ts         # 세그먼트 → 스크린(스캔라인) 투영
    renderRoad.ts         # 스캔라인 채우기(노면, 차선, 갓길)
  world/
    chunks.ts             # 청크(구간) 생성: 시드 + 바이옴 프리셋 → 세그먼트 + 오브젝트 배치
    biomes/
      countryside.ts      # 시골: 논밭, 나무, 전봇대, 작은 표지판, 현수막
      city.ts             # 도시: 건물, 네온, 육교, 대형 전광판, 버스
      highway.ts          # (후순위) 고속도로: 방음벽, 톨게이트, 대형 간판
    props.ts              # 도로변 오브젝트 정의(스프라이트, 폭, 광고 슬롯 여부)
    traffic.ts            # 차량 스폰/이동/추월 AI
    sky.ts                # 하늘 그라데이션, 별, 은하, 달/태양
    background.ts         # 원경 시차 레이어
  cockpit/
    dashboard.ts          # 대시보드, 계기판 바늘(프레임 선택), 경고등
    mirrors.ts            # 백밀러/사이드미러 렌더(후방 세그먼트 → 소형 버퍼 → 반전 블릿)
    wheel.ts              # 핸들 프레임(커브에 따라 -N..+N 단계)
  ads/
    surfaces.ts           # AdSurface 추상화: 월드 좌표의 사각 슬롯(전광판/현수막/차량 옆면)
    pixelize.ts           # 이미지 → 슬롯 크기 리샘플 + 팔레트 양자화 + 디더
    providers/
      selfServe.ts        # 자체 광고 매니페스트(JSON) 기반
      overlay.ts          # DOM 오버레이 슬롯(AdSense 등 iframe 광고용)
  ui/
    settings.ts           # 설정 패널(시간대/지역/속도/시드/CRT 효과)
    hud.ts
public/
  sprites/                # 스프라이트시트 PNG + JSON
  palettes/
  ads/                    # 자체 광고 이미지 + manifest.json
  ads.txt
```

---

## 4. 시스템 상세

### 4.1 월드 생성 (무작위 풍경)

- 월드는 길이 `N` 세그먼트의 **청크**들을 이어 붙인 무한 스트림. 앞쪽 청크는 미리 생성, 지나간 청크는 폐기.
- 청크 생성 입력: `(globalSeed, chunkIndex, biome)` → 결정적 출력. 같은 URL이면 같은 드라이브.
- 각 바이옴은 **프리셋 테이블**을 가진다:
  - 도로 형태 분포: 직선 / 완만한 커브 / 급커브 / 언덕 / S자. 도시는 직선+완만, 시골은 언덕+긴 커브 비중 높게.
  - 오브젝트 밀도·종류: 시골(전봇대 매 X m, 나무 군락, 논, 버스정류장, 현수막), 도시(건물 연속, 가로등 촘촘, 육교, 네온사인, 전광판)
  - 특수 이벤트(낮은 확률): 터널, 다리, 톨게이트, 휴게소, 대형 전광판 구간
  - 교통량: 도시 > 고속도로 > 시골
- 바이옴 간 전환은 전환 청크(교외)로 부드럽게.

### 4.2 시간대

`day` / `dusk` / `night` 세 프리셋(요청은 낮/밤이지만 노을을 추가하면 팔레트 시스템 검증에 좋고 비용이 거의 없음).

- 팔레트 램프 교체: 하늘, 원경, 노면, 콕핏 톤
- 밤: 별+은하 하늘, 전조등/미등 발광, 가로등 원뿔, 건물 창 불빛, 네온
- 낮: 태양 위치, 구름, 그림자(단순 오프셋 스프라이트)
- 노을: 그라데이션 하늘, 긴 그림자, 일부 불빛 켜짐
- 자동 순환 옵션(실시간 시계 연동 또는 N분 주기)은 후순위

### 4.3 콕핏

참고 영상 구도 그대로: 화면 하단 중앙에 계기판, 우측 상단에 백밀러, 좌측 하단에 사이드미러, 좌우에 A필러.

- 속도계/RPM: 속도 값 → 바늘 각도 → 가장 가까운 프리렌더 프레임(예: 24단계)
- 핸들: 도로 곡률에 따라 ±7단계 프레임. 자동 주행이므로 커브에 살짝 반응
- 계기판 라이트 깜빡임, 방향지시등(차선 변경 시)
- 백밀러: 후방 세그먼트 미니 렌더. 뒤에서 오는 차의 전조등이 보이면 밤 분위기가 크게 산다
- 와이퍼/비(후순위 날씨 옵션)

### 4.4 교통

- 차량은 세그먼트 위치 + 차선 + 속도. 플레이어보다 느린 앞차(미등), 반대편 차(전조등), 추월 차
- 스프라이트: 후면/전면/약간 옆면 3종 × 차종 4~6종 × 스케일 단계. 원본은 가장 가까운 크기 1개만 그리고 나머지는 빌드 시 최근접 리샘플 → 이후 손보정
- 플레이어 차선 변경은 자동(앞차 근접 시 추월). 충돌 없음(분위기 사이트이므로)

---

## 5. 광고 통합 설계

### 5.1 AdSurface 추상화

광고가 "풍경의 일부"가 되려면 광고 자리가 처음부터 **월드 오브젝트**여야 한다.

```ts
interface AdSurface {
  id: string
  kind: 'billboard' | 'banner' | 'neon' | 'busSide' | 'gantry'
  slotPx: { w: number; h: number }   // 슬롯 내부 픽셀 크기(1x 기준, 예: 48×24)
  segment: number                    // 월드 위치
  side: 'left' | 'right' | 'overhead'
  content: AdContent | null
}
```

- 전광판(billboard): 기둥+프레임+조명 스프라이트 안에 슬롯. 밤에는 상단 조명이 슬롯을 비춤(디더 하이라이트)
- 현수막(banner): 전봇대/가로등 사이에 걸린 천. 바람에 흔들리는 2~3프레임
- 네온(neon): 도시 건물 벽면. 깜빡임
- 버스/트럭 옆면(busSide): 지나가는 차량에 붙음
- 육교 간판(gantry): 도로 위를 가로지름. 가장 크고 오래 보임

슬롯은 **표준 광고 비율**(예: 2:1, 3:1, 1.2:1 ≈ 300×250)에 맞춰 설계해 어떤 공급자든 매핑되게 한다.

### 5.2 두 가지 광고 경로 (둘 다 지원)

**A. 자체 서빙(self-serve) — 완전 통합**
- `public/ads/manifest.json`에 광고 이미지·링크·가중치 나열
- 로드 시 `pixelize.ts`가 이미지를 슬롯 크기로 최근접 리샘플 → 현재 팔레트로 양자화 → 디더. 결과는 인덱스 버퍼라 격자·팔레트가 완벽히 일치
- 스케일 단계별 버전도 미리 생성(멀리서는 흐릿한 색 덩어리, 가까워지면 읽힘)
- 클릭: 슬롯의 스크린 사각형을 매 프레임 기록 → 캔버스 클릭 시 히트테스트 → 링크 열기
- 직접 영업하는 스폰서, 자기 제품, 친구 프로젝트 등에 적합. **가장 예쁘고 제약 없음**

**B. 광고 네트워크(AdSense 등) — 프레임 통합**
- AdSense 광고는 iframe이고, 정책상 **광고 소재를 변형·가리기·오해 유발 배치가 금지**된다. 캔버스에 픽셀화해 그리는 것은 불가. 따라서 광고 자체는 DOM에 원본 그대로 두고, **그 주변**을 픽셀아트로 감싸는 방식으로 "녹아들게" 한다.
- 구현: `overlay.ts`가 특정 AdSurface(주로 gantry 또는 대형 billboard)를 **정지 슬롯**으로 지정. 그 오브젝트가 화면의 정해진 위치에 도달하면 도로는 계속 흐르되 슬롯 사각형은 화면에 고정된 위치(예: 우측 상단 전광판)로 렌더되며, 그 위에 같은 사각형 크기의 DOM 광고가 정확히 겹친다.
  - 캔버스 배율이 ×4일 때 슬롯 80×62px ≈ 320×250 CSS px → AdSense `300×250` 유닛과 맞음. 배율별로 슬롯 크기를 표로 정의
  - 전광판 프레임·기둥·조명·야간 발광은 캔버스에 픽셀로 그림. 광고는 그 안의 "스크린"
- 정책 체크리스트: 광고 위에 어떤 캔버스 요소도 덮지 않음(콕핏 A필러 등과 겹치지 않는 위치), 광고 주변 클릭 유도 금지, 한 화면에 광고 수 제한, `ads.txt`, 개인정보처리방침 페이지, 쿠키 동의(EU)
- 대안 네트워크: EthicalAds, Carbon 등 **API로 소재를 받아 직접 렌더**할 수 있는 네트워크는 A 경로에 가깝게 통합 가능(각 네트워크의 렌더링 가이드라인 확인 필요)

**권장 순서**: A를 먼저 완성해 광고 슬롯 시스템 자체를 검증 → B는 M6에서 오버레이 슬롯 1개로 시작.

---

## 6. 마일스톤

각 마일스톤은 배포 가능한 상태로 끝낸다.

> **진행 상황 (2026-09-03)**: M0~M6 1차 구현 완료. 스프라이트는 전부 코드 생성 플레이스홀더(문자열 아트 + 절차 생성)이며, M7 항목(터널, 날씨, 오디오, 세로 화면 최적화, 손그림 스프라이트 교체)은 미착수.

### M0 — 뼈대 (반나절)
- Vite+TS 스캐폴드, 320×180 인덱스 프레임버퍼, 팔레트 LUT, 정수 업스케일 + 레터박스, 고정 타임스텝 루프, FPS 디버그
- 검증: 창 크기 어떻게 바꿔도 픽셀이 균일한 정사각형

### M1 — 도로 (1~2일)
- 세그먼트, 곡선/언덕 생성, 스캔라인 렌더, 차선·갓길, 하늘 디더 그라데이션, 원경 시차 1겹
- 자동 주행(일정 속도), 커브 시 원경 이동
- 검증: 스크린샷 확대 시 모든 픽셀이 격자에 정렬

### M2 — 스프라이트 & 풍경 (2~3일)
- 스프라이트 로더, 스케일 단계, 블릿/클리핑
- 도로변 프롭(가로등, 전봇대, 나무, 표지판), 시드 기반 청크 생성기, 시골 바이옴
- 설정: `?seed=`

### M3 — 콕핏 & 교통 (2~3일)
- 대시보드/계기판/핸들/미러 스프라이트, 계기판 애니메이션
- 백밀러 후방 렌더
- 차량 스프라이트, 앞차/반대차/추월

### M4 — 시간대 & 도시 (2~3일)
- 낮/노을/밤 팔레트, 밤하늘(별+은하), 조명 효과(전조등 원뿔, 미등 번짐, 가로등)
- 도시 바이옴(건물, 창 불빛, 네온, 육교), 바이옴 전환 청크
- 설정: `?time=`, `?scene=`

### M5 — UI & 마감 (1~2일)
- 설정 패널(시간대/지역/속도/시드/CRT 스캔라인 옵션), URL 동기화, localStorage
- 모바일 대응(세로 화면은 레터박스 또는 240×135 폴백), 탭 비활성 시 루프 정지
- 오디오(선택), 로딩 화면, OG 이미지
- 첫 배포

### M6 — 광고 (2~3일)
- AdSurface 시스템, 전광판/현수막/네온/버스 슬롯
- 자체 서빙 매니페스트 + 픽셀화 파이프라인 + 클릭 히트테스트
- DOM 오버레이 슬롯 1개(gantry) + AdSense 연동 준비(정책 페이지, ads.txt)

### M7 — 폴리시 (지속)
- 날씨(비/안개), 터널, 시간 자동 순환, 고속도로 바이옴, CRT 셰이더(WebGL 업스케일), 성능 프로파일링

---

## 7. 결정 사항 / 확인 필요

아래는 내가 가정한 것들이다. 다르면 알려달라.

1. **"밤/야간 선택"은 "낮/밤 선택"으로 해석**했고, 노을을 하나 더 넣었다.
2. **운전은 자동**(사용자 조작 없음). 방향키로 차선 변경 정도는 M7에서 검토.
3. **핸들 위치**: 참고 영상은 일본(우핸들, 좌측통행). 한국 기준 **좌핸들·우측통행**으로 만든다. 구도상 계기판이 좌측으로 오고 백밀러·사이드미러 위치가 바뀐다. 원본 구도를 그대로 원하면 우핸들로 간다.
4. **기본 해상도 320×180**. 더 정밀한 픽셀아트를 원하면 480×270(작업량 약 2배).
5. **스프라이트 제작**: 초기에는 코드로 생성한 플레이스홀더(단색 사각형/단순 도형)로 시스템을 완성하고, 이후 Aseprite로 진짜 스프라이트를 교체한다. 직접 그릴지, 외주/에셋 구매인지에 따라 M2~M4 일정이 달라진다.
6. **광고는 자체 서빙(A)을 1순위**, AdSense는 오버레이 슬롯으로 제한. AdSense 소재를 픽셀화하는 것은 정책 위반 소지가 있어 하지 않는다.
